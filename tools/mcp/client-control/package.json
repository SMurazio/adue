{
  "name": "mmo-client-control-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Self-authored MCP server that proxies the Godot client's localhost debug control channel (DebugControlChannel.cs) as agent tools. Loopback-only; no shell/filesystem exposure.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "server.js",
  "bin": {
    "mmo-client-control-mcp": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.8"
  }
}
