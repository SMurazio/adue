# N — ADUE P2: the stranger-duo demo slice (GATES all further investment)

From `docs/duo-standalone-plan.md` P2 — blocked on P1 (the chassis). The kill test all three
design reviews converged on: package P1 + the duo kit + the Sunderer as a downloadable
two-player build and put it in front of 5-10 duos who don't know the developer.

- Minimal onboarding: teach Q/R/G/V + pairing without a manual (teach labels exist; a first-run
  hint pass may be enough).
- Build packaging: Godot export + the server bundled/launchable for a host (the bundled
  host-side server task may land here or be faked with a "host runs a .cmd" for the demo).
- **Between-runs presentation = a MENU, not a walkable hub** (`docs/duo-living-tower.md`,
  "A in doctrine, B in the build"): title → pair → run → end screen → again. A base camp is the #1
  scope-leak vector for this phase's committed-low-fi art budget. The entire tower commitment here is
  numbered floors (`Floor 1… Floor 2… SUNDERER`) + an end-screen strip of how high the pair got.
  NO extract/haul language (nothing to extract yet; it primes testers to hunt for missing
  persistence/loot). Pitch surface says "you two," lead with the fusion moment — not the tower.
- Instrument the two pass criteria from the duo framework: do stranger duos attempt the Perfect
  version unprompted, and do they laugh? (Plus: where do they bounce?)

> **Reservation carried forward (user, 2026-08-09):** the menu feels "slightly impersonal" for a
> relationship game — a correct instinct. It's a P2 expedient, NOT the final form. If the kill-test
> passes, the first between-runs investment to revisit is a small shared "base camp / commit-together"
> space (more personal; great once couch mode exists). See `docs/duo-living-tower.md` open question.

## Blocked

Blocked on `S-adue-p1-run-loop-chassis.md`. Do not start until P1 is merged and feel-tested.
